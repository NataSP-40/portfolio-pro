# Frontend API base for Axios calls (must include /api)
VITE_API_URL=http://localhost:8000/api

# Optional override for building media URLs when backend returns relative paths.
# Leave empty in most cases; defaults to VITE_API_URL without /api.
VITE_MEDIA_BASE_URL=
